import { useEffect } from "react";

export default function FishFood() {
    useEffect(() => {
        window.scrollTo(0, 0);
      }, []);

    return (
        <>
            fish food

        </>
    )
}
